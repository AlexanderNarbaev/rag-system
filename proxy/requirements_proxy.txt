# proxy/requirements_proxy.txt
# Зависимости для RAG-прокси (FastAPI, Qdrant, эмбеддеры, реранкеры, LangGraph)

# Веб-фреймворк
fastapi>=0.139.0
uvicorn[standard]>=0.24.0
sse-starlette>=1.6.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Векторная база и эмбеддинги
qdrant-client>=1.10.0
sentence-transformers>=2.2.0
# CrossEncoder included in sentence-transformers package

# Кэш (Redis)
redis>=4.5.0
aiohttp>=3.9.0  # для асинхронных запросов к LLM (>=3.9 fixes CVE-2024-23829, CVE-2024-23334)

# Агентная оркестрация (LangGraph)
langgraph>=1.2.8
langchain-core>=0.2.0  # требуется для LangGraph

# Граф знаний (Neo4j, опционально)
neo4j>=5.14.0

# Утилиты
requests>=2.31.0
tiktoken>=0.5.0  # для точной токенизации (опционально)
filelock>=3.12.0
PyJWT>=2.8.0

# Для работы с датами
python-dateutil>=2.8.2

# Observability
prometheus-client>=0.25.0

# Auth & User Management
aiosqlite>=0.19.0
bcrypt>=4.1.0
ldap3>=2.9.0

# Дообучение моделей (Model Evolution)
peft>=0.12.0
mlflow>=2.11.0
boto3>=1.34.0
rouge-score>=0.1.2
# bert-score>=0.3.13  # опционально — для NLI-based оценки качества ответов

# Опциональные GPU-зависимости (требуются для дообучения на GPU)
# Установка: pip install -r requirements_proxy_gpu.txt
# bitsandbytes>=0.43.0  # опционально — 4-bit/8-bit квантизация для QLoRA fine-tuning
# accelerate>=0.30.0    # опционально — распределённое обучение на GPU/TPU
