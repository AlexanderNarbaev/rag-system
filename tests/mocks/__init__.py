# tests/mocks/__init__.py
"""Shared test mocks for RAG system.

Provides mock implementations of external dependencies that allow tests to run
without requiring real services (LangGraph, Qdrant, Neo4j, etc.).
"""
